//Create a program to find the length of a string using strlen. 
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello";

    printf("Length: %lu", strlen(str));

    return 0;
}
